"""Tests for SocTalk SecOps Agent.

## Triage flight recorder: pipeline visualization at two altitudes (replay + live fleet)
A "flight recorder" visualization of the AI triage pipeline, at two altitudes over one shared stage map, each with two clocks:

- **Investigation replay** (detail page): timed playback of one investigation's journey across the pipeline map — arrival, policy gate, supervisor dispatch, verdict assembly, the guard check, resolution — with a narration rail, verdict panel, and cost meter. Closed investigations replay a finished film; open ones render their journey so far with a live head.
- 
"""